[package]
name = "exg"
description = "EXG — exchange format library for LSL recordings (XDF writer + shared sample traits)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
